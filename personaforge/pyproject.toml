[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "personaforge"
version = "1.0.0"
requires-python = ">=3.9"
dependencies = ["anthropic>=0.40.0"]

[project.scripts]
pf = "pf:main"

[tool.setuptools]
py-modules = ["pf"]
